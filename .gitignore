# ── Secrets ──
.env
*.key

# ── Node / web ──
node_modules/
web/dist/
*.local

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── Pipeline ──
# The scraping + extraction stage stays local. Nobody needs it to run the site:
# its only output, web/public/data.js, is committed. Keeping it out also keeps
# full scraped song texts from being redistributed here.
pipeline/

# ── Editor / OS ──
.claude/settings.local.json
.vscode/
.idea/
.DS_Store
Thumbs.db
